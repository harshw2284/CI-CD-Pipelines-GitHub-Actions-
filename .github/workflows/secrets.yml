name: Secrets

on: 
    workflow_dispatch:

jobs:
    check-secret:
        runs-on: ubuntu-latest
        steps:
            - name: Print Secret
              run: Secret is ${{secrets.MY_SECRET_MESSAGE}}
