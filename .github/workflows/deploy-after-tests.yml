name: Ddeploy after test

on:
    workflow_run:
        workflows: ["Run Tests"]
        types: [completed]


jobs:
    deploy:
        if: ${{ github.event.workflow_run.conclusion == 'success' }}
        runs-on: ubuntu-latest
        steps: 
            - name: Deploy Application
              run: |
                echo "Test Passed"
                echo "Application Deployed"
